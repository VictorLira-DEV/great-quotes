Route -> it is used to define the route
Switch --> it allows our website to have only one active route at the same time sot it stops when it has a match
'quotes/:quoteId' -> this means we'll have a dynamic segment
